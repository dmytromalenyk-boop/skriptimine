#root kasutaja
insyalli - apt-get install git
# tavakasutajana:
esmakordselt:

a) seadista - naara kasutajanimi, email, editor
git config --global user.name "Ees Perenimi"
git config --global user.email kasutaja@nimi
git config --global core.editor nano

b) loo katalog, kus hakkad striptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git süstooni jaoks antud kaesta
git init 

remote seakistamine:
e) git remote add nimi github:aasress
git remote add skriptimine git@github.com:kasutajanimi/reponimi.git

b) git remote kontroll
git remote -v

põhikasutamine:

a) pane fail jälgimisele:
git add failinimi
